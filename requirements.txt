# Runtime and test/lint dependencies for the SAS-to-PySpark migration project.
# Pinned for reproducible local runs and CI. PySpark 3.5.x supports Python
# 3.8-3.12 and Java 8/11/17, matching the CI matrix.

# Core engine
pyspark==3.5.3

# Testing
pytest==8.3.4
chispa==0.10.1        # DataFrame-comparison helper for parity assertions

# Linting / formatting
flake8==7.1.1
black==24.8.0        # last release supporting Python 3.8 (CI matrix)
